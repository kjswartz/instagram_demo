<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to image_tag("instagram-logo-icon.png", width: '24'), root_path, class: "navbar-brand" %>
    </div>
    <%= link_to "Create Post", new_post_path, class: "btn btn-primary btn-xs navbar-btn" %>

      <% if signed_in? %>
        <p class="navbar-text navbar-right">Signed in as <%= link_to current_user.name, account_edit_path %> <% if current_user.admin? %>| <%= link_to "Admin", admin_root_path %><% end %></p>
      <% end %>
  </div>
</nav>
