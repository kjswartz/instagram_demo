<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to image_tag("instagram-logo-icon.png", width: '24'), root_path, class: "navbar-brand" %>
    </div>
      <% if signed_in? %>
        <p class="navbar-text navbar-right">Signed in as <%= link_to current_user.name, account_edit_path %></p>
      <% end %>
  </div>
</nav>
