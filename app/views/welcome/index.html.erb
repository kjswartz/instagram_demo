<% @posts.each do |post| %>
  <%= render :partial => 'posts/post', locals: {post: post} %>
<% end %>

<div class="row">
  <div class="col-md-3 text-right">
  </div>
  <div class="col-md-6 text-center">
    <%= link_to_next_page @posts, 'Load More', class: "btn btn-primary" %>
  </div>
  <div class="col-md-3">
  </div>
</div>
